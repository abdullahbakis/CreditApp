package practicum.creditapp.creditenum;

/**
 * This enum defines the possible results of a credit application.
 * A credit application can be approved or rejected, which are the only two possible values.
 */
public enum CreditResult {
    APPROVAL,
    REJECTION
}
